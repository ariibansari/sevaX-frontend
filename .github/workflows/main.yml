name: 🚀 Deploy React app

on:
  push:
    branches:
      - master

jobs:
  build-and-deploy:
    name: 🛠 Build and Deploy
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: SFTP uploader
        uses: SamKirkland/FTP-Deploy-Action@v4.3.4
        with:
          port: 21
          host: 'ftp://217.21.95.247'
          username: 'u641483924.u641483924'
          password: 'Arib@123'
          remoteDir: './sevax'
          localDir: './build'
          dryRun: false
